{
  "name": "gorgona-chat",
  "version": "1.0.0",
  "private": true,
  "description": "Gorgona Chat — public web chat with unique invite links, Cloudflare Durable Objects and Supabase.",
  "main": "src/index.mjs",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types",
    "test:syntax": "for f in src/*.js src/*.mjs public/app.js; do node --check \"$f\" || exit 1; done",
    "test:unit": "node --import ./test/register-shims.mjs --test test/*.test.mjs",
    "test": "npm run test:syntax && npm run test:unit",
    "build:check": "wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}